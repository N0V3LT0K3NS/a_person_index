## Capability-first planning

1. inspect the current host abstractly
2. map available tools into capability records
3. classify the run mode
4. if the work is contextual or pairwise, choose a comparison shape and run comparison preflight
5. choose the next surface:
   - `$a-person-index` for comparative work
   - `$a-person-index-actualization` for artifact realization
6. if the path is already artifact-shaped, use `recommend-path` and then a workflow recipe rather than rebuilding the sequence by hand

## Good compact output

State:

- run mode
- comparison shape and preflight status when relevant
- key capabilities available here
- next surface, protocol, or workflow recipe
- why that is the smallest sound path
