# Mappings

This directory stores house mappings from canonical framework objects into motifs.

Mappings are where the repo starts acting like a Rosetta layer: they encode direct, partial, inverse, symbolic, or incommensurable relationships without forcing false equivalence.
